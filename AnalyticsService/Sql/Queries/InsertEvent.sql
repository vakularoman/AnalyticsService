INSERT INTO analytics.events
(
    date,
    event_type,
    vsl_name,
    buyer_name,
    fb_id,
    session_id,
    click_id,
    video_length
)
VALUES
(
    @Date,
    @EventType,
    @VslName,
    @BuyerName,
    @FbId,
    @SessionId,
    @ClickId,
    @VideoLength
);